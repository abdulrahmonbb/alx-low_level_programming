#include "main.h"

/**
 * add - adds two integers and return the result
 * @a: first parameter
 * @b: second parameter
 * Return: Always 0
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
